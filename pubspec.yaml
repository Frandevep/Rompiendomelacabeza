name: crypto_dollar_app
description: A new Flutter project.
version: 1.0.0+1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.12.0 <3.0.0"

flutter:
  uses-material-design: true